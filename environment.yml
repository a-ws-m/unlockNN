name: unlockGNN
channels:
  - defaults
dependencies:
  - cloudpickle
  - matplotlib
  - numpy
  - pandas
  - pip
  - pyarrow
  - python=3.8
  - seaborn
  - tensorflow-gpu
  - pip:
      - matminer
      - megnet
      - mlflow
      - pymatgen
      - smact
      - tensorflow-probability
      - tqdm
prefix: /home/alex/miniconda3/envs/sse_gnn
