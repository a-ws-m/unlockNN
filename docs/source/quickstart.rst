Quickstart
==========

Install unlockGNN as per the :ref:`installation instructions<Installation>`.
It is highly recommended to use a dedicated machine with a graphics card for training.
An example model with uncertainty quantification for predicting perovskite formation
energies can be setup using the following code:

.. literalinclude:: ../../examples/joint_model.py
